<?php namespace Modules\User\Entities;

use Modules\Core\Entities\Eloquent\CoreModel;

/**
 * Class Friend
 * @package Modules\User\Entities
 * @author Huy D <huydang1920@gmail.com>
 * @copyright (c) Motila Corporation
 */
class Friend extends CoreModel {
    /**
     * The table associated with the model.
     * @var string
     */
    protected $table = 'user__friends';

    /**
     * The attributes that are mass assignable.
     * @var array
     */

    protected $fillable = [
        'user_id',
        'friend_id',
        'connected'
    ];

    protected $hidden = [];

    /**
     * The attributes that should be casted to native types.
     * @var array
     */
    protected $casts = [
        'user_id'   => 'integer',
        'friend_id' => 'integer',
        'connected' => 'boolean',
    ];

    public function user() {
        return $this->belongsTo('Modules\User\Entities\Sentinel\User', 'user_id');
    }

    public function friend() {
        return $this->belongsTo('Modules\User\Entities\Sentinel\User', 'friend_id');
    }
}
